# Used by docker compose for the backend service (copy to ".env" in this directory).
OPENAI_API_KEY=sk-...

# Comma-separated browser origins allowed to call the API (include your Railway frontend URL in production).
CORS_ORIGINS=http://localhost:3000
